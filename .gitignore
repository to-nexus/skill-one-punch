# secrets — never commit
.env
.env.local
.env.*.local
.auth/
.session/

# tooling
node_modules/
yarn.lock
pnpm-lock.yaml
*.log
.DS_Store
.tmp/

# OS
Thumbs.db

# Secrets/session/auth artifacts — NEVER commit (added by security audit)
!.env.example
.auth
.sessions/
*.session
